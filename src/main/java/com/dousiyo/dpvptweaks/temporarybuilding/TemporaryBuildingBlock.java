package com.dousiyo.dpvptweaks.temporarybuilding;

import net.minecraft.world.level.block.Block;

/** Marker block for match-scoped temporary structures. */
public final class TemporaryBuildingBlock extends Block {
    public TemporaryBuildingBlock(Properties properties) {
        super(properties);
    }
}
